{% extends 'index.html.twig' %}
{% block container %}
    <div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>
            CLUB
        </h1>
        <ol class="breadcrumb">
            <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
            <li><a href="#">CLUB</a></li>
            <li class="active">CLUB LIST</li>
        </ol>
    </section>

    <!-- Main content -->
    <section class="content">

    <!-- Default box -->
    <div class="box">
    <div class="box-header with-border">
        <h3 class="box-title">CLUB LIST</h3>


            <div class="box-tools">
                <form method="POST">
                <div class="input-group input-group-sm" style="width: 150px;">
                    <input type="text" name="nom" class="form-control pull-right" placeholder="Search">
                    <div class="input-group-btn">
                        <button type="submit" class="btn btn-default"><i class="fa fa-search" value="rechercher"></i></button>
                    </div>
                </div>
                </form>
            </div>








        </form>
    <div class="box-body">







        </form>
    </div>









    <div class="box-body">
    <table id="example2" class="table table-bordered table-hover">
        <thead>
                <tr>
                  <th>ID</th>
                  <th>NAME</th>
                  <th>DESCRITPTION</th>
                  <th>UPDATE</th>
                  <th>DELETE</th>
                </tr>
    {% for club in clubs %}
                <tr>
                  <td>{{ club.id }}</td>
                  <td>{{ club.nom }}</td>
                  <td>{{ club.description }}</td>
                  <td><a href="{{ path('club_update',{'id':club.id}) }}'"><span class="label label-success">UPDATE</span></a></td>
                  <td><a href="{{ path('club_delete',{'id':club.id}) }}'"><span class="label label-danger">DELETE</span></a></td>

                </tr>













            <!-- /.box-body -->

          <!-- /.box -->




    {% endfor %}
              </table>
{% endblock %}