{% extends 'user.html.twig' %}
{% block container %}
    <!DOCTYPE html>
    <html>
    <head>
        <title> list </title>
    </head>
    <body>
    <div class="col-md-12">
        <div class="card">
            <div class="card-header card-header-primary">
                <h4 class="card-title ">Club</h4>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table">

                        <tbody>
                        {% for media in clubs %}
                            <tr>
                                <td>{{ media.Club }}</td>
                                <td><img src="{{ asset('/img/' ~ media.img) }}" height="250px" width="250px"></td>
                                <td><img src="{{ asset('/img/' ~ media.img1) }}" height="250px" width="250px"></td>
                                <td><img src="{{ asset('/img/' ~ media.img2) }}" height="250px" width="250px"></td>
                                <td><img src="{{ asset('/img/' ~ media.img3) }}" height="250px" width="250px"></td>

                            </tr>


                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>



    </body>
    </html>
{% endblock %}